import 'package:flutter/material.dart';

class CaringScreen extends StatelessWidget {
  const CaringScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
