import 'package:flutter/material.dart';

class DiagnossScreen extends StatelessWidget {
  const DiagnossScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
